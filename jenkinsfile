pipeline {
    agent {
        docker {
            image 'node:6-alpine' 
            args '-p 3000:3000 -e HOME=/tmp -e NPM_CONFIG_PREFIX=/tmp/.npm'
        }
    }
    stages {
        stage('Build') { 
            steps {
                sh 'npm install' 
            }
        }
    }
}